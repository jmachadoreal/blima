import { CSS } from 'stitches-native'

export const containerStyles: CSS = {
  borderWidth: 1,
  width: 200,
  ml: 12
}

export const iconStyles: CSS = {
  width: '$xl',
  height: '$xl',
  br: '$full'
}
